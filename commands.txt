#build de docker image
docker build --tag docker-marketplace .


docker run -p 8080:8080 marketplaceapp

docker-compose build
docker-compose up

#inside de container

#look the proccess that are working
ps fax

#POSTGRES
PGADMIN PASSWORD: E7+-KIAJS

go build -v -gcflags='all=-N -l' main.go


env GOOS=linux GOARCH=amd64 go build main.go

sam build
sam local start-api
sam local invoke marketplace -e /events/projects.json
